//! Expiration support is implemented in `Store::expire_before` in `lib.rs`.
//!
//! This module intentionally contains no public API; it reserves the implementation unit while
//! the complete library rewrite restores the seeded compatibility method.
